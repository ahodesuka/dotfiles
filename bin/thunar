#!/bin/sh

THUNAR="/usr/bin/thunar"
start=true

for pid in $(pgrep $(basename $THUNAR)); do
    if [ "$pid" != "$$" ]; then
        start=false
        break
    fi
done

if $start; then
    $THUNAR --daemon &
fi

$THUNAR "$@"
